# ForgeDesk Production Environment
# Copy this file to .env and fill in the values before running docker compose.
# Place this .env alongside docker-compose.prod.yml on the VPS.
#
# Generate APP_KEY with:
#   docker run --rm ghcr.io/jkweks/forgedesk-web:latest php artisan key:generate --show

APP_NAME=ForgeDesk
APP_KEY=base64:REPLACE_WITH_GENERATED_KEY
APP_URL=https://yourdomain.com

# Database — used by both the postgres container and the app container
DB_DATABASE=forgedesk
DB_USERNAME=forgedesk
DB_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# Trust the Nginx Proxy Manager container so Laravel generates https:// URLs
# and reads the real client IP from X-Forwarded-For
TRUSTED_PROXIES=*

# Sanctum token lifetime in minutes (480 = 8 hours)
SANCTUM_EXPIRATION=480
